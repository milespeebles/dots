# options
# set -g terminal-overrides ",xterm-256color:Tc"
# set -g default-terminal "xterm-256color"
set-option -ga terminal-overrides ",xterm-256color:Tc"
setw -g monitor-activity on
set -g visual-activity on
set -g repeat-time 0
setw -g mode-keys vi
set -g base-index 1
set -g focus-events on
set-window-option -g automatic-rename off
set-window-option -g display-panes-time 1500
set-window-option -g other-pane-height 25
set-window-option -g other-pane-width 80
# set-option -g default-command "/bin/bash -c 'which reattach-to-user-namespace >/dev/null && exec reattach-to-user-namespace $SHELL -l || exec $SHELL -l'"

# quiet
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

# color
set -g status-fg  "#cccccc"
set -g status-bg  "#44475a"
set -g pane-border-style "fg=#44475a"
set -g pane-active-border-style "fg=#ff92d0"

# statusbar
set -g status-position top
set -g status-justify centre
set -g status-interval 1
set -g status-left "#(python3 $HOME/.scripts/python/get-current-song.py)"
set -g status-right "#(sh $HOME/.scripts/bash/get-current-todo.sh)\
#[fg=#272936,bg=#bd93f9] %Y-%m-%d \
#[fg=#ffffff,bg=#6272a4] %H:%M:%S "

set -g status-right-length 200
set -g status-left-length 200

# tabs
setw -g window-status-current-style "fg=#99ecfd bg=#6272a4"
setw -g window-status-current-format " #I#[fg=#ffffff]:#W#[fg=#c9a8fa]#F "

setw -g window-status-style "fg=#cccccc bg=#65686a"
setw -g window-status-format " #I#[fg=#cccccc]:#W#[fg=#c9a8fa]#F "

setw -g window-status-bell-style "fg=#ff6d67"

# mouse
set -g mouse

# keybinds
set-option -g prefix C-a
unbind-key C-a
bind-key C-a send-prefix

bind-key r source-file ~/.tmux.conf

bind-key : command-prompt
bind-key L clear-history

bind-key v split-window -h
bind-key s split-window -v
bind-key h select-pane -L
bind-key j select-pane -D
bind-key k select-pane -U
bind-key l select-pane -R

bind-key -n M-v split-window -h
bind-key -n M-s split-window -v
bind-key -n M-h select-pane -L
bind-key -n M-j select-pane -D
bind-key -n M-k select-pane -U
bind-key -n M-l select-pane -R
bind-key -n M-x kill-pane
bind-key -n M-n next-window
bind-key -n M-p previous-window

# bind -n C-h run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys C-h) || tmux select-pane -L"
# bind -n C-j run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys C-j) || tmux select-pane -D"
# bind -n C-k run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys C-k) || tmux select-pane -U"
# bind -n C-l run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys C-l) || tmux select-pane -R"
# bind -n C-\ run "(tmux display-message -p '#{pane_current_command}' | grep -iqE '(^|\/)vim$' && tmux send-keys 'C-\\') || tmux select-pane -l"
# bind C-l send-keys 'C-l'

bind-key C-o rotate-window

bind-key + select-layout main-horizontal
bind-key = select-layout main-vertical

# 2 x 1 x 2
bind-key 8 select-layout "cd40,181x55,0,0{50x55,0,0[50x27,0,0,13,50x27,0,28,41],78x55,51,0,21,52x55,130,0[52x27,130,0,28,52x27,130,28,42]}"

# 2 x 1 x 3
bind-key 9 select-layout "1467,181x55,0,0{50x55,0,0[50x27,0,0,31,50x27,0,28,32],79x55,51,0,0,51x55,131,0[51x18,131,0,1,51x18,131,19,4,51x17,131,38,5]}"

# 3 x 1 x 3
bind-key 0 select-layout "f6c1,181x55,0,0{50x55,0,0[50x18,0,0,31,50x18,0,19,64,50x17,0,38,32],79x55,51,0,0,51x55,131,0[51x18,131,0,1,51x18,131,19,4,51x17,131,38,5]}"

bind-key a last-pane
bind-key q display-panes
bind-key c new-window
bind-key t next-window
bind-key T previous-window

bind-key [ copy-mode
bind-key ] paste-buffer

# plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

run -b '~/.tmux/plugins/tpm/tpm'
